#pragma once
// The Sound_Core interface is shared by every platform -- see
// ../common_sound_core.h (and ../common_sound_core.cpp for the one
// implementation behind it).
#include "../common_sound_core.h"
